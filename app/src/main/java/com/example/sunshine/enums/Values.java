package com.example.sunshine.enums;

public enum Values {;
  public enum Mode{
    JSON,
    XML,
    HTML;
  }
  public enum Unit {
    CELSIUS,
    FAHRENHEIT,
    KELVIN;
  }
  public enum Lang {
    AR,
    EN;
  }
  public enum Type {
    CURR,
    MIN,
    HOUR,
    DAY,
    ALERT;
  }
}
